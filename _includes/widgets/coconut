<!-- 椰子索引组件 -->
<!-- 数据来源：include.component.items (label, url) -->

<style>
    /* 所有样式限定在 .coconut-container 作用域内 */
    .coconut-container {
        --coco-bg-color: #F8F2F5;
        --coco-active-color: #DDD4ED;
        --coco-text-color: #1c1917;
        --coco-tag-bg: #fff;
        --coco-tag-text: #5D4037;
        --coco-tag-shadow: rgba(93, 64, 55, 0.2);

        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 1.5em 0;
        padding: 20px 0;
    }

    /* 网格布局 */
    .coconut-container .coco-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
        gap: 50px 30px;
        width: 100%;
        max-width: 800px;
        padding: 0 20px;
        justify-items: center;
    }

    /* 椰子单元 */
    .coconut-container .coco-item {
        position: relative;
        width: 120px;
        height: 120px;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        -webkit-tap-highlight-color: transparent;
        transform: rotate(-5deg);
        /* 进场动画 */
        animation: coco-drop-in 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) backwards;
    }

    /* 依次延迟进场 */
    .coconut-container .coco-item:nth-child(1) { animation-delay: 0.1s; }
    .coconut-container .coco-item:nth-child(2) { animation-delay: 0.2s; }
    .coconut-container .coco-item:nth-child(3) { animation-delay: 0.3s; }
    .coconut-container .coco-item:nth-child(4) { animation-delay: 0.4s; }
    .coconut-container .coco-item:nth-child(5) { animation-delay: 0.5s; }
    .coconut-container .coco-item:nth-child(6) { animation-delay: 0.6s; }
    .coconut-container .coco-item:nth-child(7) { animation-delay: 0.7s; }
    .coconut-container .coco-item:nth-child(8) { animation-delay: 0.8s; }
    .coconut-container .coco-item:nth-child(9) { animation-delay: 0.9s; }
    .coconut-container .coco-item:nth-child(10) { animation-delay: 1.0s; }
    .coconut-container .coco-item:nth-child(11) { animation-delay: 1.1s; }
    .coconut-container .coco-item:nth-child(12) { animation-delay: 1.2s; }

    /* Emoji 本体 */
    .coconut-container .coco-emoji {
        font-size: 100px;
        line-height: 1;
        filter: drop-shadow(0 10px 15px rgba(0,0,0,0.15));
        transition: filter 0.3s ease;
        user-select: none;
    }

    /* 序号标签 */
    .coconut-container .coco-tag {
        position: absolute;
        bottom: 10px;
        right: 0;
        min-width: 32px;
        height: 32px;
        padding: 0 8px;
        background: var(--coco-tag-bg);
        color: var(--coco-tag-text);
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: inherit;
        font-size: 1.1rem;
        font-weight: 800;
        box-shadow: 0 4px 8px var(--coco-tag-shadow);
        border: 2px solid #fff;
        transform: rotate(5deg);
        transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        z-index: 10;
    }

    /* Hover 状态 */
    .coconut-container .coco-item:hover {
        transform: scale(1.1) rotate(0deg) translateY(-10px);
        z-index: 20;
    }

    .coconut-container .coco-item:hover .coco-emoji {
        filter: drop-shadow(0 20px 25px rgba(0,0,0,0.2));
    }

    .coconut-container .coco-item:hover .coco-tag {
        background: var(--coco-active-color);
        color: #4a148c;
        transform: scale(1.1) rotate(-5deg);
    }

    /* 点击状态 */
    .coconut-container .coco-item:active {
        transform: scale(0.95) translateY(0);
    }

    .coconut-container .coco-item:active .coco-emoji {
        filter: drop-shadow(0 5px 5px rgba(0,0,0,0.2));
    }

    /* 进场动画 */
    @keyframes coco-drop-in {
        0% { opacity: 0; transform: translateY(-50px) rotate(-20deg); }
        100% { opacity: 1; transform: translateY(0) rotate(-5deg); }
    }

    /* 移动端响应 */
    @media (max-width: 600px) {
        .coconut-container .coco-grid {
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
        }
        .coconut-container .coco-emoji {
            font-size: 90px;
        }
    }
</style>

<div class="coconut-container">
    <div class="coco-grid">
        {% for item in include.component.items %}
        <div class="coco-item" data-url="{{ item.url | prepend: site.baseurl }}">
            <div class="coco-emoji">🥥</div>
            <div class="coco-tag">{{ item.label }}</div>
        </div>
        {% endfor %}
    </div>
</div>

<script>
(function() {
    // 敲击椰子音效（Web Audio API 合成）
    var cocoAudioCtx = null;

    function getCocoAudioCtx() {
        if (!cocoAudioCtx) {
            cocoAudioCtx = new (window.AudioContext || window.webkitAudioContext)();
        }
        return cocoAudioCtx;
    }

    function playCocoSound() {
        var ctx = getCocoAudioCtx();
        if (ctx.state === 'suspended') {
            ctx.resume();
        }
        var now = ctx.currentTime;

        var osc = ctx.createOscillator();
        var gain = ctx.createGain();

        osc.type = 'triangle';
        osc.frequency.setValueAtTime(300, now);
        osc.frequency.exponentialRampToValueAtTime(50, now + 0.1);

        gain.gain.setValueAtTime(0, now);
        gain.gain.linearRampToValueAtTime(0.8, now + 0.01);
        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.15);

        var filter = ctx.createBiquadFilter();
        filter.type = 'lowpass';
        filter.frequency.value = 800;

        osc.connect(filter);
        filter.connect(gain);
        gain.connect(ctx.destination);

        osc.start(now);
        osc.stop(now + 0.2);
    }

    function knockCoconut(item) {
        var url = item.getAttribute('data-url');
        var emojiDiv = item.querySelector('.coco-emoji');

        // 播放敲击音效
        playCocoSound();

        // 椰子变饮料彩蛋
        var originalEmoji = emojiDiv.innerText;
        emojiDiv.innerText = '🧉';

        // 动画结束后跳转
        setTimeout(function() {
            emojiDiv.innerText = originalEmoji;
            if (url) {
                window.location.href = url;
            }
        }, 600);
    }

    // 事件委托
    var containers = document.querySelectorAll('.coconut-container');
    for (var i = 0; i < containers.length; i++) {
        containers[i].addEventListener('click', function(e) {
            var item = e.target.closest('.coco-item');
            if (item) {
                knockCoconut(item);
            }
        });
    }
})();
</script>
